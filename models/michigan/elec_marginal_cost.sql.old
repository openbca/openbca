MODEL(
    name michigan.elec_marginal_cost,
    kind SEED (
        path '$root/input/elec_marginal_cost.csv'
    ),
    columns (
        region VARCHAR,
        utility VARCHAR,
        marginal_cost FLOAT
    )
);
